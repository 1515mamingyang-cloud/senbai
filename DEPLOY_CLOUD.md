# 森柏 - 微信云托管部署指南

## 整体流程

```
微信云托管控制台
├── 1. 开通环境（免费3个月）
├── 2. 创建 MySQL 数据库
├── 3. 上传后端代码 → 自动构建 Docker 镜像 → 部署
├── 4. 配置环境变量（大模型 key、管理员账号等）
├── 5. 获取服务访问地址
└── 6. 小程序里填入这个地址
```

---

## 第一步：开通微信云托管环境

1. 打开 https://cloud.weixin.qq.com ，用你的微信扫码登录
2. 点击「开通环境」
3. 选择「按量计费」（新用户首环境赠送3个月免费额度）
4. 环境名称随便填，比如 `senbai`
5. 创建完成后进入控制台

> **免费额度（3个月内有效）**：
> - 容器 CPU：720 核·小时（够 0.25 核跑约 120 天）
> - 容器内存：1440 GB·小时（够 0.5GB 跑约 120 天）
> - MySQL：720 实例·小时（够 24 小时跑约 30 天）
> - MySQL 存储：720 GB·小时
> - 公网流量：5GB

---

## 第二步：创建 MySQL 数据库

1. 在云托管控制台左侧菜单 → 「数据库」→「MySQL」
2. 点击「新建 MySQL 实例」
3. 配置：
   - 版本：MySQL 8.0
   - 字符集：utf8mb4
   - 密码：设一个密码并记下来
4. 创建完成后，记录以下信息（在实例详情页能看到）：
   - 内网地址（如 `10.0.1.35`）
   - 端口（默认 `3306`）
   - 用户名（默认 `root`）
   - 密码（你刚设的）

5. 点击「数据库管理」→ 新建数据库，命名为 `senbai`

---

## 第三步：上传后端代码

### 方式 A：通过控制台上传（推荐，最简单）

1. 在云托管控制台 → 「服务」→「新建服务」
2. 服务名称填 `senbai-api`
3. 来源选择「代码上传」
4. 把 `senbai/backend/` 目录下的**所有文件**打包成 zip
   - 注意：不要包含 `.venv/` 目录
   - 不要包含 `senbai.db` 文件
5. 上传 zip 包
6. 系统会自动识别 `Dockerfile` 并构建镜像
7. 等待构建完成（约 2~3 分钟）

### 方式 B：通过 Git 仓库（适合后续更新）

1. 把 `senbai/backend/` 推到 GitHub/Gitee 仓库
2. 在云托管控制台 → 新建服务 → 来源选「代码库」
3. 授权并选择你的仓库
4. 后续 push 代码会自动触发重新部署

---

## 第四步：配置环境变量

在服务详情页 → 「环境变量」→ 添加以下变量：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `MYSQL_HOST` | 你的 MySQL 内网地址 | 第二步记录的 |
| `MYSQL_PORT` | `3306` | |
| `MYSQL_USER` | `root` | |
| `MYSQL_PASSWORD` | 你的 MySQL 密码 | 第二步设的 |
| `MYSQL_DATABASE` | `senbai` | 第二步创建的库名 |
| `SECRET_KEY` | 一串随机字符串 | JWT 签名密钥，用 `openssl rand -hex 32` 生成 |
| `LLM_BASE_URL` | `https://api.deepseek.com/v1` | DeepSeek 接口 |
| `LLM_API_KEY` | `sk-你的key` | 你的大模型 API key |
| `LLM_MODEL` | `deepseek-chat` | |
| `CRAWL_CRON_HOUR` | `8` | 每天几点抓取 |
| `INIT_ADMIN_USERNAME` | `mamingyang` | 首次启动自动创建管理员 |
| `INIT_ADMIN_PASSWORD` | `123456` | 管理员密码 |

> **重要**：设置完环境变量后需要重新部署服务才能生效。

---

## 第五步：获取服务访问地址

1. 在服务详情页 → 「服务设置」或「访问路径」
2. 你会看到一个类似这样的地址：
   ```
   https://senbai-api-xxxx.x-x.weixin-cloud.com
   ```
3. 测试是否正常：
   ```bash
   curl https://senbai-api-xxxx.x-x.weixin-cloud.com/
   # 应该返回：{"status":"ok","service":"森柏 API","version":"0.1.0"}
   ```

> **关键优势**：这个地址自带 HTTPS，且**不需要域名备案**，小程序可以直接调用。

---

## 第六步：修改小程序配置

打开 `senbai/miniprogram/utils/api.js`，把第一行的地址改成你的云托管服务地址：

```javascript
const BASE_URL = 'https://senbai-api-xxxx.x-x.weixin-cloud.com'
```

然后在微信开发者工具里点「上传」→ 去小程序后台设为体验版。

---

## 费用说明

| 资源 | 免费额度 | 用完后费用 | 你的用量预估 |
|------|---------|-----------|-------------|
| 容器 CPU | 720 核·小时（3个月内） | 0.055 元/核·小时 | 0.25核×24h×30天 = 180核·小时/月 ✅ 免费够用 |
| 容器内存 | 1440 GB·小时（3个月内） | 0.032 元/GB·小时 | 0.5GB×24h×30天 = 360GB·小时/月 ✅ 免费够用 |
| MySQL 算力 | 720 实例·小时（3个月内） | 0.342 元/实例·小时 | 24h×30天 = 720小时/月 ⚠️ 约第30天后开始计费 |
| MySQL 存储 | 720 GB·小时（3个月内） | 0.00485 元/GB·小时 | 1GB×24h×30天 = 720小时/月 |
| 公网流量 | 5GB（3个月内） | 0.8 元/GB | 内部小范围用，5GB够 |

**结论**：
- 前 30 天完全免费
- 第 31 天起 MySQL 约 **¥246/月**（0.342×24×30）
- 容器部分前 3 个月免费，之后约 **¥65/月**（0.055×0.25×24×30 + 0.032×0.5×24×30）

**如果觉得 MySQL 太贵**，30 天后可以：
1. 迁移到腾讯云/阿里云轻量服务器（¥60/月，SQLite 零成本，但需备案或用体验版调试模式）
2. 或继续用云托管但优化 MySQL 用量（如只在工作时间运行）

---

## 常见问题

### Q: 部署后访问报 502？
等服务完全启动后再试。首次启动需要建表+初始化数据，约 10~30 秒。

### Q: MySQL 连接失败？
检查环境变量是否正确设置，特别是 `MYSQL_HOST` 要用内网地址（不是公网地址）。

### Q: 定时任务不执行？
云托管容器可能缩容到 0 实例。确保 `container.config.json` 里 `minNum` 设为 `1`（已设好）。

### Q: 如何添加更多用户？
方法 1：在云托管控制台的「服务」→「Web Shell」里执行：
```bash
python create_user.py 用户名 密码
```

方法 2：设置 `INIT_ADMIN_USERNAME` 和 `INIT_ADMIN_PASSWORD` 环境变量，重启服务（仅在无用户时生效）。

### Q: 如何更新代码？
重新上传代码包或 push 到 Git 仓库，云托管会自动重新构建部署。

### Q: 小程序请求报错？
在微信开发者工具 → 详情 → 本地设置 → 勾选「不校验合法域名」。正式环境需在小程序后台「服务器域名」里添加云托管地址。
